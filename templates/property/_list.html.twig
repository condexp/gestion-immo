{% extends 'base.html.twig' %}

{% block title %}Artistes
{% endblock %}

{% block body %}
	<div class="container my-5">
		<div
			class="row my-5">


			{# 	{{ dump(propertys) }} #}

			{%  for bien in propertys %}
				<div class="card border-{{ bien.title }} mb-3">
					<div class="card-body ">

						<p class="card-text">{{ bien.description }}</p>
					</div>
				</div>

			{% endfor %}
			<div class="card-body text-center ">
				<a href="{{ path('property_index') }}">Retour à la liste</a>
			</a>
		</div>
	</div>
</div>{% endblock %}
